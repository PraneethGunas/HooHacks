# PolicyPulse backend dependencies

# Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# HTTP client (tools, LLM calls, premium agent)
httpx>=0.27.0

# HTML parsing (fetch_document_text tool)
beautifulsoup4>=4.12.0

# SSE streaming
sse-starlette>=2.0.0

# NOTE: L402 payments use lnget (Go binary from lightning-agent-tools).
# No Python Lightning dependencies needed.

# LLM SDKs — not needed, we use raw httpx calls to provider APIs.
# Uncomment if Rudra switches to LangChain/LangGraph:
# openai>=1.14.0
# google-generativeai>=0.4.0
# anthropic>=0.21.0
# langgraph>=0.0.30
# langchain-core>=0.1.30
