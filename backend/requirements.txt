# PolicyPulse backend dependencies

# Framework
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
pydantic-settings>=2.2.0

# HTTP client (tools, LLM calls, premium agent)
httpx>=0.27.0

# HTML/PDF parsing (fetch_document_text tool)
beautifulsoup4>=4.12.0
pdfplumber>=0.11,<1.0

# SSE streaming
sse-starlette>=2.0.0

# Env file loading
python-dotenv>=1.0,<2.0

# Testing
pytest>=8.0,<9.0
pytest-asyncio>=0.23,<1.0

# NOTE: L402 payments use lnget (Go binary from lightning-agent-tools).
# No Python Lightning dependencies needed.

# LLM SDKs — not needed, we use raw httpx calls to provider APIs.
# Uncomment if switching to LangChain/LangGraph:
# openai>=1.14.0
# google-generativeai>=0.4.0
# anthropic>=0.21.0
# langgraph>=0.4.0
# langchain-core>=0.3.0
# langchain-google-genai>=2.0
